# Añade el nuevo Dockerfile junto con el composer.json
git add Dockerfile composer.json

# Guarda el cambio con un mensaje
git commit -m "Añadir Dockerfile para despliegue en Render"

# Sube los cambios a GitHub
git push
